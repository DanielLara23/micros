// LED conectado al pin GPIO 32
const int ledPin = 32;

void setup() {
  pinMode(ledPin, OUTPUT);
}

void loop() {
  digitalWrite(ledPin, HIGH); // Enciende el LED
  delay(100);                 // 100 ms = mitad del ciclo (5 Hz)
  digitalWrite(ledPin, LOW);  // Apaga el LED
  delay(100);
}
